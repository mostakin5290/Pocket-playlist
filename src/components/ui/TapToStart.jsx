import React from 'react'

// TapToStart kept for compatibility but intentionally does nothing.
// The UX overlay was removed per user request — this file remains
// as a no-op so imports won't crash other modules if used elsewhere.
const TapToStart = () => null

export default TapToStart
